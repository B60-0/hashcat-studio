# Hashcat Studio Frontend

React + TypeScript frontend for the Wails desktop app.

```bash
npm install
npm run lint
npm run build
```

Most app APIs are exposed by Wails from the Go backend in `../internal`.
